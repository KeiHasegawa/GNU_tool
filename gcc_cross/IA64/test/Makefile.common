%.o : %.S
	ia64-elf-as -o $@ -g $<

%.o : %.c
	ia64-elf-gcc -o $@ $< -g -c

%.elf : %.o 
	ia64-elf-ld -o $@ ../default.o $< -T ../default.x

%.objdump : %.elf
	ia64-elf-objdump -x -d -g $< > $@

%.log : %.elf
	wdog.exe -v ia64-elf-gdb -q -x gdbcom $< > $@

%.lof : %.log
	wdog.exe -v diff $< $@
	touch $@

clean:
	rm -f *.o *.s *.elf *.map *.objdump *.objcopy *.log TAGS *~
