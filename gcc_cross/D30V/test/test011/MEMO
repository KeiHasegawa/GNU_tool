本当はコマンドラインから函数を呼び出すテストをするつもりだったのだが, 期待した動作ではなかった
ので, step, where に続き finish を実行してみたがこれも期待通り動作していない.

step と where は期待した動作になっている.

生成されているコードは正しいものだから d30v-elf-gdb の問題.

main:
	add sp,sp,-24
	add r22,sp,16
	stw r61,@(r22+,r0)
	stw link,@(r22+,r0)
	or r61,r0,sp

のようにしているから現状では main の戻りアドレスを正しく算出できていない.

main の戻りアドレスは 0x110 で 0x1000-24+16 = 0x0ff8 番地に格納されている.




