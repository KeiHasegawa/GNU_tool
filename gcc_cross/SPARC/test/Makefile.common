%.o : %.S
	sparc-elf-as -o $@ -g $<

%.o : %.c
	sparc-elf-gcc -o $@ $< -c -g

%.elf : %.o 
	sparc-elf-ld -o $@ ../default.o $< -T ../default.x

%.objdump : %.elf
	sparc-elf-objdump -x -d -g $< > $@

%.log : %.elf
	wdog.exe -v sparc-elf-gdb -q -x gdbcom $< > $@

%.lof : %.log
	wdog.exe -v diff $< $@
	touch $@

clean:
	rm -f *.o *.s *.elf *.map *.objdump *.log TAGS *~
