%.o : %.S
	csky-elf-as -o $@ -g $<

%.o : %.c
	csky-elf-gcc -o $@ $< -g -c

%.elf : %.o 
	csky-elf-ld -o $@ ../default.o $< -T ../default.x

%.objdump : %.elf
	csky-elf-objdump -x -d -g $< > $@

%.log : %.elf
	wdog.exe -v csky-elf-gdb -q -x gdbcom $< > $@

%.lof : %.log
	wdog.exe -v diff $< $@
	touch $@

clean:
	rm -f *.o *.s *.elf *.map *.objdump *.objcopy *.log TAGS *~
