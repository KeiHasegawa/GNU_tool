%.o : %.S
	x86_64-elf-as -o $@ -g $<

%.o : %.c
	x86_64-elf-gcc -o $@ $< -c -g

%.elf : %.o 
	x86_64-elf-ld -o $@ ../default.o $< -T ../default.x

%.objdump : %.elf
	x86_64-elf-objdump -x -d -g $< > $@

%.log : %.elf
	wdog.exe -v x86_64-elf-gdb -q -x gdbcom $< > $@

%.lof : %.log
	wdog.exe -v diff $< $@
	touch $@

clean:
	rm -f *.o *.s *.elf *.map *.objdump *.log TAGS *~
