%.o : %.S
	sh64-elf-as -o $@ -g $<

%.o : %.c
	sh64-elf-gcc -o $@ $< -g -c

%.elf : %.o 
	sh64-elf-ld -o $@ ../default.o $< -T ../default.x

%.objdump : %.elf
	sh64-elf-objdump -x -d -g $< > $@

%.log : %.elf
	wdog.exe -v sh64-elf-gdb -q -x gdbcom $< > $@

%.lof : %.log
	wdog.exe -v diff $< $@
	touch $@

clean:
	rm -f *.o *.s *.elf *.map *.objdump *.objcopy *.log TAGS *~
