%.o : %.S
	iq2000-elf-as -o $@ $< -g

%.o : %.c
	iq2000-elf-gcc -o $@ $< -c -g

%.elf : %.o
	iq2000-elf-ld -o $@ start.o $< -T $(basename $<).x

%.objdump : %.elf
	iq2000-elf-objdump -x -d -g $< > $@

%.log : %.elf
	wdog.exe -v iq2000-elf-gdb -q -x gdbcom $< > $@

%.lof : %.log
	wdog.exe -v diff $< $@
	touch $@

clean:
	rm -f *.s *.o *.elf *.objdump *.log *.a *.map *~

