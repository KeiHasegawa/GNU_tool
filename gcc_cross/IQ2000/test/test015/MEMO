longjmp の最後の命令

	j	r31

のさらに先をフェッチしているためにマップされていない領域をアクセスしているため
エラーしている.

回避策として

sim memory-region 0x800002a8,4

とする必要がある. と思ったが

	j	r31

は遅延分岐なので nop 命令が必要. setjmp の最後の j 命令の後も同様. 

しかしこれを行なっても longjmp で指定した 123 が setjmp の戻り値になっていない.
これはシミュレータのバグっぽい.

	bne	r5,r0,1f
	li	r5,1
1:
	move	r2,r5

bne 命令を実行すると r5 = 1 になっている. と思ったが, これも遅延分岐.
なので bne 命令の次に nop を入れておいた.

