%.o : %.S
	mips-elf-as -o $@ $< --gen-debug

%.o : %.c
	mips-elf-gcc -o $@ $< -c -g

LIBGCC = /media/ca850f52-0605-42a7-9fd4-899de9edf461/work/GNU_LANG/lib/gcc/mips-elf/10.2.0/libgcc.a

%.elf : %.o
	mips-elf-ld -o $@ start.o $< $(LIBGCC) -T $(basename $<).x

%.objdump : %.elf
	mips-elf-objdump -x -d -g $< > $@

%.log : %.elf
	wdog.exe -v mips-elf-gdb -q -x gdbcom $< > $@

%.lof : %.log
	wdog.exe -v diff $< $@
	touch $@

clean:
	rm -f *.s *.o *.elf *.objdump *.log *.a *.map *~

