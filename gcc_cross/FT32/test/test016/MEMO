実行するとメモリアクセスエラーを起こすが, 直接の原因は以下のループの終了
条件を正しく判定できていない.

  for ( struct S* p = test_200 ; p != test_204 ; ++p ){

.L14:
	lda.l  $r0,test_204
	ldi.l  $r1,$fp,-4
	cmp.l  $r1,$r0

r0 が 0x800078 になるのは期待した動作だが, r1 = 0x18 になる. r1 = 0x800018
になるのが期待値.

上に先立って

	ldk.l  $r0,test_200
	sti.l  $fp,-4,$r0
	jmp    .L14

が実行されるが r0 = 0x18 になっている. 0x800018 が期待値.
しかしそもそも

	ldk.l

命令ではこうはならない.

スタートアップルーチンでも

	ldk.l	$r2, _data_start

のようにしているが本来これは r2 = 0 ではなく r2 = 0x800000 になるのが期待値.
