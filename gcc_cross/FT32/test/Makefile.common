%.o : %.S
	ft32-elf-as -o $@ $< -g

%.o : %.c
	ft32-elf-gcc -o $@ $< -c -g

%.elf : %.o
	ft32-elf-ld -o $@ start.o $< -T $(basename $<).x

%.objdump : %.elf
	ft32-elf-objdump -x -d -g $< > $@

%.log : %.elf
	wdog.exe -v ft32-elf-gdb -q -x gdbcom $< > $@

%.lof : %.log
	wdog.exe -v diff $< $@
	touch $@

clean:
	rm -f *.s *.o *.elf *.objdump *.log *.a *.map *~

