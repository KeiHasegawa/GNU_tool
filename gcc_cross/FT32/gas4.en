      emit_expr_fix (pexp, 2, frag, p, TC_PARSE_CONS_RETURN_NONE);

Above `pexp' is dumped by below debug function:

void debug(struct expressionS* expr)；

This function is called from debugger command line.

Then I found

X_add_number : 4
X_add_number : 8
X_add_number : 12

I guessed above caused the problem.
