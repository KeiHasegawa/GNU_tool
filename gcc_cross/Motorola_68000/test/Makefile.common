%.o : %.S
	m68hc11-elf-as -o $@ $< -g

%.o : %.c
	m68hc11-elf-gcc -o $@ $< -c -g

%.elf : %.o
	m68hc11-elf-ld -o $@ start.o $< -T $(basename $<).x

%.objdump : %.elf
	m68hc11-elf-objdump -x -d -g $< > $@

%.log : %.elf
	wdog.exe -v m68hc11-elf-gdb -q -x gdbcom $< > $@

%.lof : %.log
	wdog.exe -v diff $< $@
	touch $@

clean:
	rm -f *.s *.o *.elf *.objdump *.log *.a *.map *~

