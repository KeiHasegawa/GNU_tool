CR16 cross compile environment

(*1) CR16
(*2) Install with script => compiler internal error


(*1) CR16
https://en.wikipedia.org/wiki/CompactRISC

(*2) Install with script => compiler internal error

Use the same script with "IQ2000":

set TARGET = cr16-elf   # Change

But at gcc-10.2 install, while compiling runtime library, target compiler(xgcc) 
caused internal error.

You can see the log at Jpanese page.
