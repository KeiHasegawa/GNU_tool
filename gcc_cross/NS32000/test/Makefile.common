%.o : %.S
	ns32k-elf-as -o $@ -g $<

%.o : %.c
	ns32k-elf-gcc -o $@ $< -g -c

%.elf : %.o 
	ns32k-elf-ld -o $@ ../default.o $< -T ../default.x

%.objdump : %.elf
	ns32k-elf-objdump -x -d -g $< > $@

%.log : %.elf
	wdog.exe -v ns32k-elf-gdb -q -x gdbcom $< > $@

%.lof : %.log
	wdog.exe -v diff $< $@
	touch $@

clean:
	rm -f *.o *.s *.elf *.map *.objdump *.objcopy *.log TAGS *~
