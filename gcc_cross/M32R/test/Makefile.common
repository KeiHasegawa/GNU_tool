%.o : %.S
	m32r-elf-as -o $@ $< -g

%.o : %.c
	m32r-elf-gcc -o $@ $< -c -g

%.elf : %.o
	m32r-elf-ld -o $@ ../default.o $< -T ../default.x

%.objdump : %.elf
	m32r-elf-objdump -x -d -g $< > $@

%.log : %.elf
	wdog.exe -v m32r-elf-gdb -q -x gdbcom $< > $@

%.lof : %.log
	wdog.exe -v diff $< $@
	touch $@

clean:
	rm -f *.s *.o *.elf *.objdump *.log *.a *.map *~

