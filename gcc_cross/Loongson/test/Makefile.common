%.o : %.S
	loongarch64-elf-as -o $@ -g $<

%.o : %.c
	loongarch64-elf-gcc -o $@ $< -g -c

%.elf : %.o 
	loongarch64-elf-ld -o $@ ../default.o $< -T ../default.x

%.objdump : %.elf
	loongarch64-elf-objdump -x -d -g $< > $@

%.log : %.elf
	wdog.exe -v loongarch64-elf-gdb -q -x gdbcom $< > $@

%.lof : %.log
	wdog.exe -v diff $< $@
	touch $@

clean:
	rm -f *.o *.s *.elf *.map *.objdump *.objcopy *.log TAGS *~
