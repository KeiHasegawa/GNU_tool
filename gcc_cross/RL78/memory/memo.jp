RL78 シミュレータのメモリ

#define MEM_SIZE 0x100000

/* Only for cpu.c to use.  */
extern unsigned char memory[];

静的に 1 MByte のメモリを確保している.

これを止めて memory.diff のパッチを適用すれば .text, .rodata, .data, .bss に
メモリを割り当てることができる.

シミュレータのユーザはスタックやヒープにメモリを割り当てる必要がある.


