%.o : %.S
	lm32-elf-as -o $@ $< -g

%.o : %.c
	lm32-elf-gcc -o $@ $< -c -g

%.elf : %.o
	lm32-elf-ld -o $@ start.o $< -T $(basename $<).x

%.objdump : %.elf
	lm32-elf-objdump -x -d -g $< > $@

%.log : %.elf
	wdog.exe -v lm32-elf-gdb -q -x gdbcom $< > $@

%.lof : %.log
	wdog.exe -v diff $< $@
	touch $@

clean:
	rm -f *.s *.o *.elf *.objdump *.log *.a *.map *~

