Value returned has type: struct S. Cannot determine contents

のようになる. これはパッチなしの状態.

patch -p1 < ~/lang/53_GNU_tool/gcc_cross/finish/sh-tdep.diff

のようにしてパッチをあてたが, finish コマンドで

Value returned is $1 = {i = {0, 0, 0, 0, 0}}

のようになっている. fail の扱いにしておく.

