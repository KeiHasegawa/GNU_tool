%.o : %.S
	v850-elf-as -o $@ $< -g

%.o : %.c
	v850-elf-gcc -o $@ $< -c -g

LIBGCC = /media/ca850f52-0605-42a7-9fd4-899de9edf461/work/GNU_LANG/lib/gcc/v850-elf/10.2.0/libgcc.a

%.elf : %.o
	v850-elf-ld -o $@ start.o $< $(LIBGCC) -T $(basename $<).x

%.objdump : %.elf
	v850-elf-objdump -x -d -g $< > $@

%.log : %.elf
	wdog.exe -v v850-elf-gdb -q -x gdbcom $< > $@

%.lof : %.log
	wdog.exe -v diff $< $@
	touch $@

clean:
	rm -f *.s *.o *.elf *.objdump *.log *.a *.map *~

