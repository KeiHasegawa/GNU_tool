test.S にはシミュレータで正しく判定できていなかったものを集めたものに
なっている.

例えば

	srll	rr4, #5
	slll	rr4, #5

に対して両方を slll 命令として判定していた. これの経緯については

~/lang/53_GNU_tool/gcc_cross/Z8000/gdb/fix_sim.jp

を参照. 現状は命令の判定はできているが, 命令のシミュレーションで
abort() を呼び出す状態になっている. つまりまだできていない.

