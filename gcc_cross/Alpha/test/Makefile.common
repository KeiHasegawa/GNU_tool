%.o : %.S
	alpha-elf-as -o $@ -g $<

%.o : %.c
	alpha-elf-gcc -o $@ $< -g -c

%.elf : %.o 
	alpha-elf-ld -o $@ ../default.o $< -T ../default.x

%.objdump : %.elf
	alpha-elf-objdump -x -d -g $< > $@

%.log : %.elf
	wdog.exe -v alpha-elf-gdb -q -x gdbcom $< > $@

%.lof : %.log
	wdog.exe -v diff $< $@
	touch $@

clean:
	rm -f *.o *.s *.elf *.map *.objdump *.objcopy *.log TAGS *~
