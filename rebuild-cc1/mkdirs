mkdir ~/O0_build || exit 1
cd ~/O0_build || exit 1
mkdir libbacktrace || exit 1
mkdir libbacktrace/.libs || exit 1
mkdir libcpp || exit 1
mkdir libdecnumber || exit 1
mkdir libiberty || exit 1
mkdir zlib || exit 1
mkdir gcc || exit 1
mkdir gcc/analyzer || exit 1
mkdir gcc/c || exit 1
mkdir gcc/c-family || exit 1
mkdir gcc/common || exit 1
